# Kâhya MVP — Master Backlog

How to work this list: `tasks/README.md`. Design source of truth: `docs/HANDOFF.md`.
Take the **first `[ ]` task whose deps are all `[x]`**, top to bottom.

Status: `[ ]` todo · `[~]` in progress · `[!]` blocked on user · `[x]` done
Flags: 🧍 user-assist · ⏳ long-running · ↔ slidable per §6 timing note

---

## Phase W0 — Day-1 setup (`tasks/w0-setup/`, HANDOFF §7)

- [x] **W0-01** 🧍 `memory-repo-seed` — create `~/Kahya/{memory,backups}` as its own git repo; seed from `~/.claude/projects/-Users-matt-Test/memory/` (exclude MEMORY.md) + gold-token README/notes; first commit; user does the 10-minute review (surviving facts = `source_tier=user_asserted`); user supplies private remote URL. Deps: none. Refs: §7, §3.
- [x] **W0-02** `toolchain-bootstrap` — Go module + goose + sqlc scaffolding; Python env with pinned `claude-agent-sdk` + lock file; Makefile (`build/test/lint`); minimal CI-ready test harness. Deps: none. Refs: §4 stack, §9.
- [x] **W0-03** ⏳ `local-models-download` — download `Qwen3-30B-A3B` (MLX 4-bit), `whisper-large-v3-turbo` (MLX), `Qwen3-Embedding-0.6B` (~20GB, resume-capable); verify `say -v '?' | grep -i yelda` and flag for user if missing. Deps: none. Refs: §4 models, §7.
- [!] **W0-04** 🧍 `keychain-codesign` — self-signed `Kahya Dev` signing identity + Makefile codesign step; add `kahya.anthropic` / `kahya.telegram` / `kahya.anchor` Keychain items with `-T $(which kahyad)`; user supplies actual secret values. Deps: W0-02. Refs: §7 secrets, §9. *Blocked: identity trusted + `make install` verified (Authority=Kahya Dev, DR stable); user still must add `~/bin` to PATH and run the 3 `security add-generic-password` commands (kahya.anthropic needed for W1–2).*
- [!] **W0-05** 🧍 `design-doc-snapshot` — commit the full design artifact as `docs/design.html` (fetch from the artifact URL in HANDOFF §9; if unreachable, ask the user to export it). Deps: none. Refs: §7, §9. *Blocked: artifact 404s even authenticated (deleted/unshared); user must export the design artifact from the browser and save as `~/code/kahya/docs/design.html`.*

## Phase W1–2 — Core (`tasks/w1-2-core/`, HANDOFF §4 IPC, §5 schema, §6 W1–2)

- [x] **W12-01** `kahyad-skeleton` — Go daemon: config, UDS listener at `~/Library/Application Support/Kahya/kahyad.sock`, JSONL logging with `trace_id`, launchd LaunchAgent plist (`KeepAlive=true`), graceful shutdown, health endpoint. Deps: W0-02. Refs: §4 architecture+IPC.
- [x] **W12-02** `schema-migrations` — goose migrations run at kahyad startup before any work; full §5 schema (`episodes, chunks, facts, entities, entity_aliases, evidence, merge_ledger, tasks, events/outbox` — all columns incl. `source_tier`, `evidentiality`, log-odds `confidence`, `valid_from/to`, `model_ver`); WAL + `PRAGMA busy_timeout` + checkpoint policy + `PRAGMA user_version`; sqlc query layer. Deps: W12-01. Refs: §5 schema block, §4 stack.
- [x] **W12-03** `fts5-dual-index` — FTS5 dual index (`trigram` + `unicode61`) over chunks with BM25 score fusion; `sqlite-vec` (≥0.1.9) vec table with `model_ver` column (vectors filled by W12-11); search API in kahyad. Deps: W12-02. Refs: §4 stack ⚑, §6 W1–2.
- [x] **W12-04** `corpus-indexer` — index `~/Kahya/memory/*.md` into episodes/chunks (language-agnostic, file-hash incremental); `kahya reindex` trigger; kahyad sole writer. Deps: W12-03, W0-01. Refs: §4 memory, §6 W1–2.
- [~] **W12-05** `memory-mcp-server` — `memory_search` / `memory_write` / `memory_forget` MCP tools served by kahyad (Go); writes flow markdown-first then reindex; `source_tier` respected on injection eligibility. Deps: W12-04. Refs: §4, §5 memory #1.
- [x] **W12-06** `kahya-cli` — one-shot + REPL + `kahya log --trace <id>` over UDS; Turkish UX strings; W1–5 primary interaction surface. Deps: W12-01. Refs: §4 UI.
- [ ] **W12-07** `ipc-worker-spawn` — per-task worker spawn: task envelope JSON on stdin, `trace_id` env/arg, `session_id` field reserved; `POST /policy/check` HTTP-over-UDS endpoint (5s timeout, **fail-closed**) with interim static policy: only `memory_search` allowed, every other tool denied until W3 lands (secret-lane classification doesn't exist yet, so unclassified file reads must not reach a cloud call — see task file). Deps: W12-01, W12-02, W12-06. Refs: §4 IPC ⚑.
- [ ] **W12-08** `anthropic-forward-proxy` — localhost forward-proxy adding auth header from Keychain; worker spawned with `ANTHROPIC_BASE_URL`; per-task 500K token ceiling, $10/day $150/month budgets, 80% downgrade rung, cache-hit metric, Telegram-alarm hooks (alarm wiring completes in W3-07); prompt-cache discipline (frozen system prefix + tool defs, 1h TTL). Deps: W12-01, W0-04. Refs: §4 IPC ⚑ + cost governor ⚑.
- [ ] **W12-09** `python-worker-harness` — `ClaudeSDKClient` **streaming input mode**; `UserPromptSubmit` hook injecting `<hafiza>` block from `memory_search`; `can_use_tool` calling `/policy/check` (early-reject layer); JSONL logs with `trace_id`; model routing table (routing decision in Go, worker obeys envelope). Deps: W12-07, W12-08, W12-05. Refs: §4 IPC ⚑ + model routing.
- [ ] **W12-10** `w12-acceptance` — end-to-end gate: CLI question answered with `<hafiza>` injection from seeded memory; **`'evlerimizden'` query finds the seed note containing `'ev'`** (trigram, no manual stemming); single `trace_id` across kahyad+worker JSONL logs; every enjected `<hafiza>` block recorded in ledger. Deps: W12-01..09. Refs: §6 W1–2 acceptance, §5 safety #4.
- [ ] **W12-11** ↔ `embedding-pipeline` — MLX embedding service (`Qwen3-Embedding-0.6B`, 512-dim MRL) supervised by kahyad; chunk embedding with `model_ver` tag; KNN filtered to single active `model_ver`; hybrid fusion with FTS5; full re-embed flow from markdown. May slide to W3–4; TR→EN cross-lingual retrieval doesn't work until this lands. Deps: W12-10, W0-03. Refs: §4 models ⚑ `model_ver` rule, §6 timing note.

## Phase W3 — Policy + tools (`tasks/w3-policy-tools/`, HANDOFF §4 ladder, §5 safety, §6 W3)

- [ ] **W3-01** `policy-yaml-loader` — `policy.yaml` schema + validating loader: R/W1/W2/W3 static labels, `reversible` + per-tool undo recipe, secret-lane globs (file paths only!), fs write-deny globs (shell rc, LaunchAgents, `~/.hammerspoon`, App Support/Kahya), egress allowlist + volume budgets. Deps: W12-01. Refs: §5 safety #1/#6 ⚑, §4 ladder.
- [ ] **W3-02** `policy-engine-ladder` — binding decision in kahyad: autonomy ladder per tool×class×scope (L0–L4), promotion suggestion at 20 consecutive approvals + 0 denials (never auto), demotion on deny/undo/violation; **one-time approval tokens** verified by side-effectful MCP tools; W1 5-min undo window; ledger events for every decision. Deps: W3-01, W12-07. Refs: §4 ladder ⚑, §5 safety enforcement plane ⚑.
- [ ] **W3-03** `fs-tool` — fs MCP tool (kahyad-owned): R/W classification, write-deny globs, undo recipes (delete→Trash, file write→pre-op git checkpoint), canonical path resolution. Deps: W3-02. Refs: §5 safety #6 ⚑ deny globs, §4 ladder undo.
- [ ] **W3-04** ⏳ `docker-shell-tool` — Docker runtime setup (colima or Docker Desktop) + sandbox image + mount policy (only task workdir rw, rest none/ro) + **default `--network none`**; narrow arg-validated host-exec set; binary allowlist is NOT a security boundary. Deps: W3-02. Refs: §5 safety #6 + #1 ⚑ container egress.
- [ ] **W3-05** `egress-proxy` — kahyad egress proxy: target allowlist + volume budget for every off-box byte (HTTP body+URL, DNS, mail); allowlist-external egress hard-blocked after sensitive reads in session; approval cards count as egress; network-needing container jobs route only through this proxy. Deps: W3-02, W3-03, W3-04. Refs: §5 safety #1 ⚑ (all three bullets).
- [ ] **W3-06** `wysiwye-approval` — approval pipeline: NFC normalize, bidi/zero-width/homoglyph strip, canonical path/host, hash of approved bytes, **executed-bytes≠approved-bytes ⇒ reject**; byte-exact diff renderer for approval surfaces. Deps: W3-02. Refs: §5 safety #5 ⚑.
- [ ] **W3-07** 🧍 `telegram-bot` — `gopkg.in/telebot.v4` inside kahyad; single fixed chat_id/user_id allowlist enforced Go-side (non-matching updates dropped silently + ledgered); **long-polling**; inline buttons for W2 approvals with byte-exact diff; W3 = notification only, never approval input; secret-lane content never sent (redacted title max); `remote` label in ledger; cost alarms delivery. Deps: W3-06, W0-04. Refs: §5 safety #5 ⚑ Telegram bullets, §4 cost governor.
- [ ] **W3-08** ⏳ `secret-lane-local` — kahyad-supervised `mlx_lm.server` (Qwen3-30B-A3B, 127.0.0.1, fixed port, health-check); on-demand load + idle-TTL unload + free-memory check (**insufficient ⇒ FAIL-CLOSED, never cloud**); local content pre-classifier at ingest for content-sourced data (mail/web); Go routing branch; "yerel işlendi" badge; **ordering invariant: no byte to cloud before local classification completes**. Deps: W3-01, W12-08, W0-03. Refs: §4 routing ⚑ ordering + memory pressure ⚑, §5 product principles.
- [ ] **W3-09** ↔ 🧍 `applescript-jxa-tools` — AppleScript/JXA/Shortcuts MCP tools: static class ≥W2, script bytes approved via WYSIWYE diff, bodies containing `do shell script`/`doShellScript` rejected or routed to Docker shell. May slide to W4–5. Deps: W3-06, W3-04. Refs: §5 safety #6 ⚑ osascript, §6 timing note.
- [ ] **W3-10** `w3-acceptance` — gate tests: W2 action requires byte-exact Telegram diff approval; **W3 cannot be approved via Telegram but passes with written CLI "onayla"**; secret-lane-touching actions fall to local approval; egress allowlist active + **in-container `curl` cannot bypass it (test)**; secret-lane content cannot reach a cloud call (test). Deps: W3-01..08. Refs: §6 W3 acceptance.

## Phase W4 — Durability (`tasks/w4-durability/`, HANDOFF §6 W4, §5 safety #2/#4)

- [ ] **W4-01** `scheduler` — wall-clock jobs via launchd `StartCalendarInterval` (missed-during-sleep runs once on wake); in-daemon `robfig/cron` only for short-interval ticks while daemon runs (Go darwin monotonic-clock sleep bug — never trust in-daemon cron for wall-clock). Deps: W12-01. Refs: §4 stack ⚑ scheduling.
- [ ] **W4-02** `task-outbox-resume` — task state machine `intent → executing → receipt` with idempotency/receipt semantics; background tasks + session resume via `session_id`; receipt-less `executing`: only W1 auto-retries, W2/W3 never; outbox drives redelivery. Deps: W12-07, W12-02. Refs: §6 W4 ⚑, §4 IPC.
- [ ] **W4-03** `taint-reader-actor` — two-LLM split: toolless **Reader** parses untrusted bytes → Go-side struct/schema-validated output (free-text fields length+charclass constrained); privileged **Actor** never sees raw untrusted text; W-actions only from a CLEAN new Actor session seeded with validated output; taint persisted in SQLite keyed by `session_id`, reloaded on resume, **only ever rises**, missing record ⇒ untrusted (fail-closed); untrusted tier = R-tools + notify only. Deps: W12-09, W3-02, W3-06, W3-08, W4-02. Refs: §5 safety #2 ⚑ (all bullets).
- [ ] **W4-04** `cloud-error-taxonomy` — retryable classes (429/5xx/network) + exponential backoff + max attempts + `bekliyor-yeniden-deneme` task state; non-retryable surfaces clean Turkish error; Fable-5 calls always with server-side fallback beta + Opus fallback per §4. Deps: W12-08, W4-02, W12-09, W3-08. Refs: §6 W4, §4 routing.
- [ ] **W4-05** 🧍 `ledger-external-anchor` — every N hours kahyad writes last ledger event hash to an append-only, separately-credentialed remote (append-only deploy-key git repo or S3 Object Lock; offline: different-uid local append-only file); anchor identity = separate Keychain item read only by anchor code path; mismatch detection ⇒ alarm; user supplies the remote. Deps: W12-02, W0-04, W4-01. Refs: §5 safety #4 ⚑.
- [ ] **W4-06** `backups` — nightly `VACUUM INTO ~/Kahya/backups/brain-YYYYMMDD.db` + `PRAGMA integrity_check`, keep 7; Time Machine excludes live WAL db, includes VACUUM copy; nightly `git push` of `~/Kahya`; Keychain-loss runbook (rotate API keys). Deps: W4-01, W0-01. Refs: §6 backup ⚑.
- [ ] **W4-08** `intent-router` — the §4 model-routing table in Go: task-type→model selection (Opus plan / Sonnet fan-out / Haiku extraction), local-Qwen intent classification (<300ms warm; single combined call extending W3-08's classifier — fleet stays at three models), `--derin` flag + byte-exact `derin düşün:` prefix opt-in pinning `claude-fable-5` (W4-04 proxy shaping guarantees the fallback beta + Opus fallback), cost-governor 80% downgrade rung acting on the table incl. the Sonnet→yerel rung (retires W12-08's `budget_downgrade_unavailable`); secret-lane pin outranks everything; worker keeps obeying the envelope. Deps: W12-07, W3-08, W4-04. Refs: §4 model routing (table + ⚑ ordering invariant + Fable-5 rule), §4 cost governor ⚑.
- [ ] **W4-07** `w4-acceptance` — gate tests: ≥10-min task calling a W2 tool SIGKILLed mid-tool-call resumes to completion **without double execution** (verified via outbox/ledger); command issued offline completes on reconnect or fails with explicit notification; local ledger tamper detected against remote anchor + alarm. Deps: W4-01..06. Refs: §6 W4 acceptance.

## Phase W5 — Proactivity + consolidation (`tasks/w5-proactivity/`, HANDOFF §6 W5, §5 memory)

- [ ] **W5-01** 🧍 `morning-briefing` — 08:30 briefing via scheduler (no Graph API: `gh` + files + local calendar); single notification with `trace_id`; briefing session is tainted **by design** ⇒ produces notifications only, direct W-tool calls from it are rejected. Deps: W4-01, W4-03. Refs: §6 W5, §5 safety #2.
- [ ] **W5-02** `nightly-consolidation` — Agent SDK session merges/organizes markdown, git-commits; **suggestion-mode for first 2 weeks** (diff + user approval; auto-commit only after W7 mini-eval green); commit discipline: dirty tree → `author=user` commit first, daemon changes always separate `author=kahyad` commit, user_edit wins conflicts (daemon skips lines user touched that day); consolidation writes ONLY markdown+git, kahyad triggers reindex; every summary from raw evidence, never from sub-summaries; ≥90-day hot window + detail-atom promotion (numbers/dates/quotes/decisions/promises) before cooling. Deps: W4-01, W12-04, W12-09. Refs: §6 W5 ⚑, §5 memory #4.
- [ ] **W5-03** `truth-ritual` — weekly light true/false ritual over Telegram (~10 facts, reuses W3-07 bot); answers update fact confidence (negative evidence lowers, <0.3 exits injection); labels accumulate as the W7–8 eval set; owns the "hatırladı anı" marking flow (§6 metric ⚑): `🌟 Hatırladı` inline button on Telegram task/ritual messages + `kahya remembered --trace <id>` → `remembered_moment` events rows read by W78-04 and gated by W78-06 (≥5/week, §9). Deps: W3-07, W12-02, W12-06. Refs: §6 W5 ⚑, §5 memory #3/#5, §6 metrik tanımları ⚑, §9 MVP-done.
- [ ] **W5-04** `memory-correctness-engine` — fact extraction/update logic enforcing §5 memory rules: source-trust lattice (`user_edit`›`user_asserted`›`external_doc`›`screen`›`agent_derived`, agent-derived quarantined from profile card/injection until user confirms); log-odds confidence, same-session repeats = one evidence, retraction handling; evidence-gated entity merge (never name-similarity alone, Turkish namesakes), merge ledger + entity-split op, suspicious same-name ⇒ new provisional entity; evidentiality from -mış morphology (witnessed|reported|inferred). Deps: W5-02. Refs: §5 memory #1–#4 ⚑.
- [ ] **W5-05** 🧍 `w5-acceptance` — gate tests: 08:30 briefing = single notification + `trace_id`; nightly consolidation produces diff commit; **tainted briefing session's direct W-tool call rejected, same action passes from clean session**; ~20-question retrieval mini-baseline shows no regression after consolidation. Deps: W5-01..04. Refs: §6 W5 acceptance.

## Phase W6 — Voice + shortcut (`tasks/w6-voice/`, HANDOFF §6 W6)

- [ ] **W6-01** 🧍 `hammerspoon-palette` — `⌥Space` global palette + approval cards (`hs.notify` with action buttons, local approval surface from W6 on) + Accessibility/mic permissions checklist (test under launchd, not terminal). Deps: W12-06, W3-06. Refs: §4 UI, §7 TCC checklist ⚑.
- [ ] **W6-02** `ptt-whisper` — push-to-talk → `mlx-whisper` as **library in worker** (`whisper-large-v3-turbo`, `language=tr` fixed) → transcript → task loop, 100% local. Deps: W6-01, W0-03. Refs: §4 stack STT, §6 W6.
- [ ] **W6-03** `halt-semantics` — `⌥⎋`: Hammerspoon→kahyad 'halt' IPC → kill worker process-group + related Docker containers; task terminal state `user_halted` (**permanently excluded from session-resume and outbox retry**); all pending approvals invalidated. Deps: W6-01, W4-02. Refs: §6 W6 ⚑.
- [ ] **W6-05** `tts-yelda` — spoken answers/notifications via `say -v Yelda` behind a config toggle (`tts.enabled`, default off; `kahya ask --speak` one-shot override): local notification path only (never Telegram), text via stdin, serialized utterances, halt (`⌥⎋`) kills in-flight speech, secret-lane results not spoken by default, missing voice ⇒ degrade silent + single Turkish notification (§4 ⚑ Yelda note, W0-03 flags). Deps: W6-01, W0-03. Refs: §4 stack TTS ⚑, §4 IPC bildirim teslimi ⚑, §8.
- [ ] **W6-04** `w6-acceptance` — gate tests: hold→speak→transcript→task loop fully local; **`⌥⎋` during long task ⇒ task does NOT continue or retry even after daemon restart**; palette-open→first-token timestamps logged to events table. Deps: W6-01..03. Refs: §6 W6 acceptance.

## Phase W7–8 — Hardening + eval (`tasks/w7-8-hardening/`, HANDOFF §6 W7–8)

- [ ] **W78-01** `eval-retrieval-qa` — ~50 real Turkish/mixed-language command set + retrieval QA eval (labels from W5-03 ritual); precision ≥80% including abstention; gate wired to run before any consolidation/embedding/fusion change. Deps: W5-05, W12-11. Refs: §6 W7–8, §5 memory #5.
- [ ] **W78-02** `redteam-dev-profile` — red-team eval set (planted-mail-poisons-profile, web-page-exfiltrates-memory, homoglyph-approval-bypass, **tainted-session-still-tainted-after-restart**) running ONLY under `KAHYA_ENV=dev` (separate brain.db + `~/Kahya-dev/memory` + egress deny-all + separate launchd label + record-replay SDK fixtures); target: 0 successful bypasses. Deps: W4-07, W3-10, W5-05. Refs: §6 W7–8 ⚑.
- [ ] **W78-03** `invariant-ci-tests` — every §5 invariant (safety #1–#6, memory #1–#5, product principles) mapped to at least one permanent code test; all collected in CI (`make test` + CI workflow); coverage map committed as `docs/coverage.md`. Deps: W3-10, W4-07, W5-05. Refs: §5 preamble ⚑, §6 W7–8.
- [ ] **W78-04** `metrics-cli` — metrics from events table: commands/day, clarification-turn rate, palette-open→first-token p50, "it remembered" moments (user-marked via W5-03's flow), cache-hit rate, daily spend; `kahya metrics` command. Deps: W12-06, W12-02, W12-08, W5-03, W6-04. Refs: §6 metric definitions ⚑ + north star.
- [ ] **W78-05** `backup-restore-drill` — rehearse full restore on a clean environment: fresh clone of `~/Kahya` + latest `brain-*.db` backup ⇒ same query yields same `<hafiza>` injection; write the runbook `docs/restore-runbook.md`. Deps: W4-06, W12-10, W78-02. Refs: §6 W7–8 acceptance, §6 backup ⚑.
- [ ] **W78-06** `dogfood-readiness` — final checklist against ALL §6 acceptance gates + MVP-done criteria (§9); open a 2-week dogfood tracking doc (`docs/dogfood.md`: daily commands, remembered-moments, incidents); rule: **nothing from v1 starts until MVP survives 2 weeks of real daily use**. Deps: everything above. Refs: §2, §6 north star, §9.

---

**Total: 50 tasks.** Weekly gates are the `*-acceptance` tasks — do not start the next phase until the current gate is `[x]` (exceptions: ↔-flagged tasks per §6 timing note). Note on row order vs IDs: W4-08 and W6-05 were added by the 2026-07-06 gap-fix pass and sit **before** their phase gates (W4-07, W6-04) so the "first `[ ]` whose deps are all `[x]`" rule completes them before the gate closes the phase.
