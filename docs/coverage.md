# Kâhya backlog coverage map — HANDOFF requirement → task(s)

> Generated by the completeness critic (2026-07-06) against `docs/HANDOFF.md` v0.2 and the task
> files under `tasks/` (now 50 after the same-day gap-fix pass added W4-08 `intent-router` and
> W6-05 `tts-yelda` and resolved all five open gaps). Status: **covered** / **partial** / **GAP**.
> Note: W78-03 will later extend this file into an invariant→test map; this version is the
> requirement→task map it starts from.

## §4 locked decisions (⚑ items)

| Requirement | Ref | Task(s) | Status |
|---|---|---|---|
| IPC contract frozen in W1–2 (per-task spawn, envelope JSON stdin, trace_id env, session_id) | §4 IPC ⚑ | W12-07 (`docs/ipc.md`) | covered |
| `ClaudeSDKClient` streaming input + `UserPromptSubmit` + `can_use_tool` | §4 IPC ⚑ | W12-09 | covered |
| `POST /policy/check` HTTP-over-UDS, 5s timeout, fail-closed | §4 IPC ⚑ | W12-07, W12-09; test in W78-03 | covered |
| kahyad supervises MLX processes; `mlx_lm.server` 127.0.0.1, pinned port, health-check | §4 IPC ⚑ | W12-11 (`mlxsup`), W3-08 | covered |
| `mlx-whisper` as library inside worker (not a server) | §4 IPC ⚑ | W6-02 | covered |
| API key never to worker; forward-proxy + `ANTHROPIC_BASE_URL`; governor/cache/egress gate at proxy | §4 IPC ⚑ | W12-08 (+W3-08 backstop) | covered |
| Notification delivery: `hs.notify` local, Telegram remote, background results carry `trace_id` | §4 IPC ⚑ | W6-01, W3-07 | covered |
| All processes JSONL with `trace_id` every line | §4 IPC ⚑ | W12-01, W12-09 | covered |
| Action classes R/W1/W2/W3 as static tool metadata | §4 ⚑ | W3-01 | covered |
| Autonomy ladder per tool×class×scope; promotion SUGGESTED at 20/0, never auto; demotion; W3 always approval | §4 ⚑ | W3-02 | covered |
| `reversible` flag + per-tool undo recipes (Trash, git checkpoint); W1 5-min window | §4 | W3-01, W3-02, W3-03 | covered |
| goose migrations at kahyad startup; `PRAGMA user_version` | §4 stack ⚑ | W12-02 | covered |
| FTS5 dual index trigram+unicode61, BM25 fusion; sqlite-vec ≥0.1.9 | §4 stack ⚑ | W12-03 (+W12-11 KNN leg) | covered |
| Scheduling: launchd `StartCalendarInterval` for wall-clock; in-daemon cron only for ticks | §4 stack ⚑ | W4-01 | covered |
| SQLite outbox (embedded NATS = v2) | §4 stack | W4-02 | covered |
| STT `whisper-large-v3-turbo`, `language=tr` fixed, PTT | §4 stack | W6-02 | covered |
| TTS `say -v Yelda`: Day-1 voice verification ⚑ | §4 stack ⚑ | W0-03 | covered |
| TTS `say -v Yelda`: actually wiring spoken output (MVP TTS choice) | §4 stack | W6-05 (config-toggled local speech, `--speak` override, degrade-silent on missing voice; W6-02's out-of-scope note now points there) | covered |
| Model routing decision in Go, worker obeys envelope | §4 routing | W12-07/W12-09 (static default), W4-08 (dynamic table) | covered |
| Intent router / dynamic routing table (Opus plan / Sonnet fan-out / Haiku extraction / local Qwen <300ms classification / "derin düşün" Fable-5 opt-in) | §4 routing | W4-08 (table in Go + combined local-Qwen intent path + `--derin`/`derin düşün:` opt-in + 80% rung on the table; W12-07/W12-09 static default until it lands) | covered |
| Ordering invariant: no byte to cloud before local secret-lane classification | §4 routing ⚑ | W3-08, W4-04 (retry path), W12-07 (interim Read-deny), W6-02 (voice) | covered |
| Fable-5 never default; always fallback beta + Opus fallback | §4 ⚑ | W4-04 (proxy shaping), W4-08 (opt-in is the only selection entry point) | covered |
| Local fleet exactly 3 models, pinned | §4 ⚑ | W0-03 (`docs/models.md`) | covered |
| `model_ver` rule: single active ver in KNN; full re-embed; eval-gated switch | §4 ⚑ | W12-11, W78-01 (gate b) | covered |
| Cost governor: 500K/task, $10/day, $150/mo, pause+Telegram, 80% downgrade rung, cache-hit alarm, prompt-cache discipline | §4 ⚑ | W12-08 (+W3-07 alarm delivery, W12-09 frozen prefix, W4-08 rung acting on the routing table incl. Sonnet→yerel) | covered |
| Memory pressure: on-demand load, idle-TTL, free-mem check, FAIL-CLOSED never cloud | §4 ⚑ | W3-08 (+W4-03, W78-03 test) | covered |

## §5 invariants

| Invariant | Task(s) | Status |
|---|---|---|
| Enforcement-plane preamble ⚑ (#0): `can_use_tool` not a boundary; binding decision in kahyad; one-time approval tokens verified by side-effectful tools | W3-02 (tokens), W12-05/W12-07 (kahyad gate), W78-03 (#0 test) | covered |
| Safety #1: egress first-class gated (allowlist + volume budget, every off-box byte) | W3-05 | covered |
| Safety #1 · container `--network none`; net jobs only via egress proxy | W3-04, W3-05 (internal network + socat fwd + curl-bypass test) | covered |
| Safety #1 · approval cards count as egress; post-sensitive-read content restraint | W3-05 (`egress.Check` used by W3-07 sender, W4-05 anchor, W5-03 ritual) | covered |
| Safety #1 · hard block of allowlist-external egress after sensitive read, session-lifetime | W3-05 (+W3-03 `secret_lane_read` seam) | covered |
| Safety #2: data-level taint + dual-LLM Reader/Actor; Actor never sees raw untrusted text | W4-03 | covered |
| Safety #2 · untrusted tier = R-tools + notify only; W-actions from CLEAN seeded session; briefing tainted by design | W4-03, W5-01 | covered |
| Safety #2 · taint persisted in SQLite by session_id, reloaded on resume, only rises, missing⇒untrusted | W4-03, W78-02 (restart scenario) | covered |
| Safety #3: source_tier + human gate — untrusted-ingested memory never in profile card / reflex injection | W12-05 (injection-eligibility quarantine), W5-04 (tier clamping, confirm flow); profile-card builder itself not in MVP | covered |
| Safety #4: external-anchored ledger; every injected `<hafiza>` block recorded | W4-05 (anchor+verify+separate Keychain item), W12-05/W12-10 (`hafiza_injected` + sha256) | covered |
| Safety #5: WYSIWYE (NFC, bidi/zero-width/homoglyph, canonical path/host, hash; executed≠approved ⇒ reject) | W3-06 (+W78-02 homoglyph scenario) | covered |
| Safety #5 · W3 "onayla" local-surface only (CLI W3–5, Hammerspoon W6+); Telegram notify-only for W3 | W3-06, W3-07, W6-01 (server-stamped surface) | covered |
| Safety #5 · secret-lane bytes never in Telegram diff (redacted title max) | W3-07 (`redact.go`), W5-01 (briefing redaction) | covered |
| Safety #5 · Telegram auth: fixed chat_id/user_id Go-side, silent drop + ledger, long-polling, `remote` label | W3-07 | covered |
| Safety #6: model shell in Docker by default (workdir-only rw, no net); binary allowlist not a boundary; narrow arg-validated host set | W3-04 | covered |
| Safety #6 · osascript/JXA/Shortcuts ≥W2, WYSIWYE-diffed, `do shell script` rejected/rerouted | W3-09 (↔ slidable; must land before W7–8) | covered |
| Safety #6 · fs write-deny globs (shell rc, LaunchAgents, .hammerspoon, App Support/Kahya) | W3-01 (globs), W3-03 (enforcement + canonicalization) | covered |
| Memory #1: source-trust lattice; agent_derived quarantined until user confirms | W5-04, W12-05, W12-04/W0-01 (seed = user_asserted) | covered |
| Memory #2: evidence-gated entity merge; split op; merge_ledger; provisional namesakes | W5-04 | covered |
| Memory #3: negative evidence + log-odds; same-session dedupe; <0.3 out of injection; retraction | W5-04, W5-03 (ritual answers) | covered |
| Memory #4: ≥90-day hot window; detail-atom promotion; summaries from raw evidence only | W5-02 | covered |
| Memory #5: eval labels from weekly human ritual; gate before every consolidation/embedding/fusion change | W5-03 (labels), W78-01 (3 gated points), W5-05 (mini-baseline) | covered |
| Schema table list open Day-1 (episodes, chunks, facts+all columns, entities, entity_aliases, evidence, merge_ledger, tasks, events/outbox) | W12-02 | covered |
| Product principle: W3 permanent written approval | W3-02, W3-06, W3-07, W78-03 | covered |
| Product principle: safety in executor (static labels, Go check before tool runs) | W12-07, W3-01/W3-02 | covered |
| Product principle: privacy in code (Go branch, "yerel işlendi" badge) | W3-08 | covered |
| Product principle: memory can never lower a permission | no earlier task names it; W78-03 must map it to a CI test (gap-fill mandate) | partial — verify W78-03 adds the test |

## §6 weekly acceptance gates

| Gate | Task(s) | Status |
|---|---|---|
| W1–2: CLI answer with `<hafiza>` from seed; `'evlerimizden'`→`'ev'`; single trace_id; blocks ledgered | W12-10 (gate) over W12-01..09; FTS-only per timing note ⚑; embedding split out as W12-11 ↔ | covered |
| W3: W2 Telegram byte-exact diff; W3 not via Telegram / CLI "onayla"; secret-lane → local approval; egress allowlist + container-curl-can't-bypass test; secret-lane can't reach cloud test | W3-10 (5 gate tests + live checklist); W3-09 correctly excluded per timing note | covered |
| W4: SIGKILL mid-W2-tool → resume w/o double execution; offline command completes/fails explicitly; ledger tamper detected vs anchor | W4-07 (A/B/C, CI-speed + real run) | covered |
| W5: single 08:30 notification + trace_id; nightly diff-commit; tainted-briefing W-tool DENY vs clean session; ~20-q mini-baseline no regression | W5-05 | covered |
| W6: PTT loop 100% local; `⌥⎋` no-continue/no-retry across daemon restart; palette→first-token timestamps in events | W6-04 | covered |
| W7–8: retrieval precision ≥80% incl. abstention; red-team 0 bypass (dev profile); §5 invariant tests in CI; restore drill once | W78-01, W78-02, W78-03, W78-05 | covered |
| Backup ⚑ block (git remote + nightly push; `VACUUM INTO` + integrity_check, 7 copies, TM exclusions; Keychain-loss runbook) | W4-06 (+W0-01 remote) | covered |
| Metric definitions ⚑ + north star (komut/gün, açıklama-turu, p50, hatırladı anı) | W78-04 (reads), W5-03 (remembered-moment emission), W78-06 (gates) | covered |
| "hatırladı anı" emission: user marks remembered moments, tied to weekly ritual | W5-03 (`🌟 Hatırladı` button on Telegram task/ritual messages + `kahya remembered --trace <id>` → idempotent `remembered_moment` events rows), W78-04 (counts) | covered |

## §7 Day-1 items

| Item | Task(s) | Status |
|---|---|---|
| Memory repo seed (rsync excl. MEMORY.md, gold-token notes, first commit) + 10-min review + tier mapping `user_asserted` | W0-01 (+W12-04 applies tier) | covered |
| ASCII dir names; code repo separate from `~/Kahya` | W0-01 | covered |
| Local model downloads (~20GB, 3 models) + Yelda voice check | W0-03 | covered |
| Keychain 3 items + `Kahya Dev` codesign, stable ACL | W0-04 | covered |
| Keychain locked ⇒ cloud fail-fast + notify, local lane keeps working | W12-08 | covered |
| TCC checklist: Hammerspoon Accessibility/Mic/Input-Monitoring; Automation per app; test under launchd | W6-01 (`docs/tcc-w6-checklist.md`), W3-09 (`docs/tcc-automation.md`), W5-01 (Calendar) | covered |
| TCC checklist: Full Disk Access for the fs tool | W6-01 (`docs/tcc-w6-checklist.md` now includes the §7 row-3 FDA section: grant to launchd-started codesigned kahyad, verification via protected-dir `fs_read`, blocked-user handling), W3-03 (`Tam Disk Erişimi gerekli: <path>` error seam) | covered |
| `docs/design.html` committed | W0-05 | covered |
| First e2e loop + first "hatırladı" moment in week 1 | W12-09 (live RoomPlan check) | covered |

## §9 MVP-done criteria

| Criterion | Task(s) | Status |
|---|---|---|
| 2 weeks uninterrupted daily use; nothing from v1 before that | W78-06 (`docs/dogfood.md`, `--phase=complete`) | covered |
| Zero data-loss; restore-from-backup drilled once | W4-06, W78-05, W78-06 (incident parsing) | covered |
| ≥10 commands/day | W78-04, W78-06 | covered |
| ≥5 "hatırladı" moments/week | W5-03 (emission: marking flow → `remembered_moment` events), W78-04 (count), W78-06 (gate) | covered |
| Egress / secret-lane / W3 invariants code-tested, green in CI | W78-03 | covered |

## Sanity checks

- **(a) Dependency graph:** no cycles. Every dependency (BACKLOG and task-file `Depends on:`) points
  to an earlier row in BACKLOG top-to-bottom order, and no task depends on a later phase's gate, so
  the "first `[ ]` whose deps are all `[x]`" rule cannot deadlock. The two ↔ tasks (W12-11, W3-09)
  are correctly excluded from their phase gates (W12-10 must not depend on vectors; W3-10 omits
  W3-09) and both land before their consumers (W78-01 ← W12-11; W7–8 ← W3-09).
  **Drift (resolved 2026-07-06):** seven BACKLOG dep lines understated the task files' fuller
  `Depends on:` lists (W12-07, W3-05, W4-03, W4-04, W4-05, W78-04, W78-05). The BACKLOG `Deps:`
  lines are now synced to the task-file headers; all synced deps point to earlier rows, so no
  cycle or reordering was introduced. The gap-fix pass also added W4-08 (deps W12-07/W3-08/W4-04)
  and W6-05 (deps W6-01/W0-03), both placed before their phase gates with earlier-row deps only,
  and W5-03 gained W12-06 (synced in both places).
- **(b) §8 "do NOT build now":** no contradictions. Every hit for NATS/SQLCipher/GPT-OSS/Endpoint
  Security/VM/wake-word/XTTS-Piper/intent-LoRA/iPhone/hash-chain/SwiftUI/bitemporal-graph/saga-
  compensator/reranker/firehose in the tasks is either a HANDOFF quote, an explicit Out-of-scope
  deferral, or fixture text (NATS/saga in test corpora). W4-05 stays at the running-digest floor;
  W5-04 populates valid_from/to columns without a graph engine; W12-04 skips FSEvents.
- **(c) Gate tasks vs §6 verbatim:** each `*-acceptance` task encodes every clause of its week's §6
  sentence (checked clause-by-clause above), most quoting the Turkish text byte-exact, and each
  converts the gate into permanent repo tests plus a recorded live run.

## Open gaps (summary)

All five gaps from the 2026-07-06 critic pass are **resolved** (same-day gap-fix pass):

1. **"hatırladı anı" marking** — W5-03 now owns emission: `🌟 Hatırladı` inline button on Telegram
   task/ritual messages + `kahya remembered --trace <id>`, writing idempotent `remembered_moment`
   events rows with `trace_id`; W78-04 counts, W78-06 gates (≥5/week, §9).
2. **TTS output** — new task **W6-05 `tts-yelda`** wires `say -v Yelda` behind a config toggle
   (local notification path only, degrade-silent on missing voice); W6-02's out-of-scope note updated.
3. **Intent router** — new task **W4-08 `intent-router`** implements the §4 routing table in Go:
   task-type→model, combined local-Qwen intent classification (<300ms warm), `--derin`/`derin düşün:`
   Fable-5 opt-in (W4-04 shaping), 80% downgrade rung on the table incl. Sonnet→yerel; W12-07/W12-09
   "post-core" notes now point to W4-08.
4. **BACKLOG dep lines** — synced to task-file `Depends on:` headers (see sanity check (a)).
5. **Full Disk Access owner** — W6-01's `docs/tcc-w6-checklist.md` deliverable now includes the §7
   row-3 FDA section (grant to kahyad under launchd, verification command, blocked-user handling),
   with a matching acceptance criterion.
